This folder stores file-backed edit state created by editor-server.js.

- Per-page inline edits are stored as: <page>.html.json
- Gallery state is stored as: gallery-photos.json

Run `node editor-server.js` and edit at `http://127.0.0.1:5500` to write updates here.
